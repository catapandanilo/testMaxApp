<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.catapan.testemaxima">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity" />
        <activity
            android:name=".main.SplashActivity"
            android:theme="@style/AppTheme.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.ClientActivity"
            android:label="@string/infoClients"></activity>

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="br.com.catapan.testemaxima.main.MainActivity" />

        <activity
            android:name=".main.OrderHistActivity"
            android:label="@string/orderOfHistory" />
        <activity
            android:name=".main.LicenseActivity"
            android:label="@string/licenses"></activity>
        <activity
            android:name=".main.LegendsActivity"
            android:theme="@style/Theme.AppCompat.Transparent.NoActionBar">

        </activity>
    </application>

</manifest>